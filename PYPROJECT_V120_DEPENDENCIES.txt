Keep the existing project version at:

version = "1.2.0"

Recommended dev dependencies for the full verification profile:

bandit>=1.8
semgrep>=1.90

Both are treated as optional by the Advanced Verification Engine so that the
core platform remains usable when they are not installed.
